name: Build iOS App with Xamarin

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: macos-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Install Mono
      run: |
        brew update
        brew install mono

    - name: Install MSBuild
      run: |
        brew install --cask visual-studio

    - name: Install NuGet
      run: |
        brew install nuget
        nuget update -self

    - name: Restore NuGet dependencies
      run: |
        nuget restore ProjectTerra.iOS/ProjectTerra.iOS.csproj

    - name: Build iOS Project
      run: |
        msbuild ProjectTerra.iOS/ProjectTerra.iOS.csproj \
          /t:Build \
          /p:Configuration=Release \
          /p:Platform=iPhoneSimulator \
          /p:OutputPath=./output

    - name: Archive and Export IPA
      run: |
        xcodebuild -exportArchive \
          -archivePath ./output/ProjectTerra.xcarchive \
          -exportPath ./output \
          -exportOptionsPlist ExportOptions.plist

    - name: Upload IPA as artifact
      uses: actions/upload-artifact@v3
      with:
        name: ProjectTerra.ipa
        path: ./output/*.ipa
